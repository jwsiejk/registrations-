name: local_elt_lab
version: 1.0.0
config-version: 2

profile: local_elt_lab

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  local_elt_lab:
    staging:
      +materialized: view
      +schema: analytics_staging
    intermediate:
      +materialized: view
      +schema: analytics_intermediate
    marts:
      +materialized: table
      +schema: analytics_marts
