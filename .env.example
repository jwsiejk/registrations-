# Local Docker Compose port mapping overrides (host:container)
CRM_DB_PORT=5433
ERP_DB_PORT=5434
WAREHOUSE_DB_PORT=5435

# Local TLS certificate validity for generated Postgres certs (days)
TLS_CERT_VALIDITY_DAYS=825
